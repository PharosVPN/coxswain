# Build output
/cox
/dist/

# Runtime state — never committed (DESIGN §10)
/state/
*.db
*.db-wal
*.db-shm

# IP-geolocation databases — fetched at runtime, never committed (license: MaxMind
# GeoLite2 is non-redistributable; DB-IP Lite is fetched by `cox geoip update`).
*.mmdb

# Operator config written by `cox init`
/coxswain.yaml

# OS / editor cruft
.DS_Store

# Live test-fleet inventory (transient droplet IPs — never committed)
/TEST-FLEET.md
bin/
ProxMoxVMs.txt
.claude/
